events {}

http {
  include mime.types;

  server {
    listen 80;

    location /graphql {
      proxy_pass http://server:4000;
    }

    location / {
      root /front/src;
      try_files $uri /index.html;
    }
  }
}
